// The kiosk's design resolution. Set this to the Figma frame size —
// the whole UI is laid out at exactly this size and scaled to fit the screen.
export const DESIGN_WIDTH = 1080
export const DESIGN_HEIGHT = 1920
